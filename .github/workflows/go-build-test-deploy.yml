name: "Test"

on:
  push:
    branches: ["*", "**"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the Code
        uses: actions/checkout@v4
      - name: Run tests
        run: GO111MODULE=off go test -v ./...
